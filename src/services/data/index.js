/**
 * Data Services Index
 *
 * Professional data infrastructure services
 */

const { FREDService, PRIORITY_SERIES } = require('./fredService');

module.exports = {
  FREDService,
  PRIORITY_SERIES,
};
